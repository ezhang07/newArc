# --- Private / local notes (kept off the repo) ---
CLAUDE.md
.claude/
./ingest/README.md

# --- Python / FastAPI / ML ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.env
.env.*
!.env.example
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Data / model artifacts (don't commit large/generated files) ---
*.sqlite
*.sqlite3
*.db
data/*
*.csv
*.parquet
*.npy
*.npz
*.pkl
*.pt
*.bin
models/

# ...but DO track the cleaned catalog — the server needs it to run.
# (works because we ignore data/* contents, not the data/ dir itself)
!data/anime.parquet

# --- Node / React ---
node_modules/
dist/
build/
.cache/
coverage/
*.local
.vite/

# --- Editor / OS ---
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
